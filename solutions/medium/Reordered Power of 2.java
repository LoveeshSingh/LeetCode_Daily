// Title: Reordered Power of 2
            // Difficulty: Medium
            // Language: Java
            // Link: https://leetcode.com/problems/reordered-power-of-2/

class Solution {
    public boolean reorderedPowerOf2(int n) {
        List<String> list = Arrays.asList(String.valueOf(n).split(""));
        Collections.sort(list);
        for (int i=0 ; i<=29 ; i++){
            num<<=1;
        }
    }
        int num = 1;
            if (list.equals(powerOf2)) return true;
        return false;
·‌·‌·‌·‌·‌·‌·‌·‌·‌·‌·‌·‌//·‌System.out.println(num);
            List<String> powerOf2 = Arrays.asList(String.valueOf(num).split(""));
            Collections.sort(powerOf2);
}