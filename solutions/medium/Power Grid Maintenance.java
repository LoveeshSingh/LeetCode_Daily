// Title: Power Grid Maintenance
            // Difficulty: Medium
            // Language: Java
            // Link: https://leetcode.com/problems/power-grid-maintenance/

        else if (map[val].isEmpty()) return -2;
        else{
            return map[val].first();
        if (map[val].contains(val)) return val;

    int get(int val){
    }   
    void remove(int val){
        map[val].remove(val);
        }
    } 

            map[i].add(i);
            map[i] = map[val];
            if (map[val]==null) map[val] =new TreeSet<>();
            int val = find(i);
        for (int i=0 ; i<parent.length ; i++){
    void build(){
        return true;
    }

            size[v]+=size[u];
        }
            parent[u] = v;
        else{
        }
            size[u]+=size[v];
            parent[v] = u;
