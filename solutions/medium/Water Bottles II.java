// Title: Water Bottles II
            // Difficulty: Medium
            // Language: Java
            // Link: https://leetcode.com/problems/water-bottles-ii/

class Solution {
    public int maxBottlesDrunk(int numBottles, int numExchange) {
        int full = numBottles;
        int empty = 0;
        int exchng = numExchange;
        while(full>0){
            while(empty>=exchng){
        }
                full+=1;
            }
    }
            ans+=full;
        int ans=0;
            empty+=full;
            full=0;
                empty-=exchng;
                exchng++;
        return ans;
}
