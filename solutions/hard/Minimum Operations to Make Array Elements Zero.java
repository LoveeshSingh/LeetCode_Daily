// Title: Minimum Operations to Make Array Elements Zero
            // Difficulty: Hard
            // Language: Java
            // Link: https://leetcode.com/problems/minimum-operations-to-make-array-elements-zero/

    public long minOperations(int[][] queries) {
        for (int[] query : queries){
            int right = query[1];
        }  
            int left = query[0];
            for (int i=1 ; i<=15 ; i++){
            }
            int pwr = 4;
                if (left<pwr){
                    if (right<pwr) val=right-left+1;
                }
    }
                pwr<<=2;
            long cnt=0;
        long ans=0;
                int val=0;
                    else val=pwr-left;
                    left=pwr;
                    cnt+=((long)val)*i;
                    if (right<pwr) break;
class Solution {
            ans+=(cnt+1)/2;
        return ans;          
            // System.out.println(cnt);
}
